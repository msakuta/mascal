fn infer() -> i32 {
    var i = 32;
    i
}

fn f2(x: i32) {
    var interim = x + 2;
    interim * 2
}

fn f3(s: str, x: i64) {
    strlen(s) + x
}

fn i64(i: i32) -> i64 {
    var ret = i as i64;
    ret
}
