struct Vec2 {
    x: f64,
    y: f64,
}

struct Mat {
    w: Vec2,
    z: Vec2,
}

// fn format_vec(s: Vec2) -> str {
//     (s.x) as str + ", " + (s.y) as str
// }

// fn format_mat(s: Mat) -> str {
//     format_vec(s.w) + ", " + format_vec(s.z)
// }

var m = Mat {
    w: Vec2 { x: 10., y: 20. },
    z: Vec2 { x: 30., y: 40. },
};
m.z.y = 400.;

print(m.w.y);

// print(format_mat(m))
